# CPE106L Lab 01 - Tools, Standards, and Versioning

## Project Overview

This project demonstrates repeatable workstation setup, virtual environment isolation, code organization (`src/` and `tests/`), and clean version control using Git.

## Setup & Running

1. Activate the virtual environment:

```bash
source .venv/Scripts/activate
